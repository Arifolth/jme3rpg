<project
        xmlns="http://maven.apache.org/POM/4.0.0"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>ru.arifolth.game</groupId>
    <artifactId>Game</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>Game</name>

    <url>https://github.com/Arifolth/jme3rpg</url>

    <developers>
        <developer>
            <id>arifolth</id>
            <name>Alexander Nilov</name>
            <email>arifolth@gmail.com</email>
        </developer>
    </developers>

    <modules>
        <module>Assets</module>
        <module>Libraries</module>
        <module>Common</module>
        <module>Modules</module>
        <module>GameClient</module>
    </modules>

    <build>
        <plugins>
            <!--<plugin>
              <groupId>org.codehaus.mojo</groupId>
              <artifactId>license-maven-plugin</artifactId>
              <version>2.0.0</version>
              <executions>
                <execution>
                  <id>aggregate-download-licenses</id>
                  <goals>
                    <goal>aggregate-download-licenses</goal>
                  </goals>
                </execution>
              </executions>
            </plugin>-->
            <plugin>
                <groupId>org.sonarsource.scanner.maven</groupId>
                <artifactId>sonar-maven-plugin</artifactId>
                <version>3.4.0.905</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.10.1</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>3.5.0</version>
                <executions>
                    <execution>
                        <id>enforce-java</id>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <requireJavaVersion>
                                    <version>[${java.version},)</version>
                                </requireJavaVersion>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!--
            <plugin>
              <artifactId>maven-source-plugin</artifactId>
              <executions>
                <execution>
                  <id>attach-sources</id>
                  <phase>verify</phase>
                  <goals>
                    <goal>jar</goal>
                  </goals>
                </execution>
              </executions>
            </plugin>
            -->
            <!--mvn project-info-reports:dependencies-->

            <!--mvn license:aggregate-add-third-party-->
            <!--mvn license:aggregate-third-party-report-->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>license-maven-plugin</artifactId>
                <version>2.1.0</version>
                <configuration>
                    <!-- Define the output file for the licenses -->
                    <licensesOutputFile>${project.basedir}/NOTICE</licensesOutputFile>

                    <!-- Specify that you want to include only direct dependencies -->
                    <includeTransitiveDependencies>false</includeTransitiveDependencies>

                    <!-- Define the format of the output file -->
                    <licensesOutputFormat>markdown</licensesOutputFormat>

                    <!-- Optionally, you can define a custom header or footer -->
                    <!--
                    <header>${project.basedir}/src/license/header.txt</header>
                    <footer>${project.basedir}/src/license/footer.txt</footer>
                    -->

                    <!-- Set to true to fail the build if there are warnings -->
                    <failIfWarning>true</failIfWarning>
                </configuration>
                <executions>
                    <execution>
                        <id>generate-notice</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>aggregate-add-third-party</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <version>3.3.1</version>
                <configuration>
                    <encoding>UTF-8</encoding>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-assembly-plugin</artifactId>
                <version>3.7.1</version>
            </plugin>
        </plugins>
    </build>

    <licenses>
        <license>
            <name>GNU General Public License, Version 3</name>
            <url>http://www.gnu.org/licenses/gpl-3.0.html</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <properties>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>

        <encoding>UTF-8</encoding>

        <jmonkey.version>3.9.0-stable</jmonkey.version>
        <jme3.groupId>org.jmonkeyengine</jme3.groupId>

        <lemur.version>1.16.0</lemur.version>
        <lemur-proto.version>1.13.0</lemur-proto.version>
    </properties>

    <profiles>
        <profile>
            <id>jdk17</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <properties>
                <java.version>17</java.version>
            </properties>
        </profile>
        <profile>
            <id>native</id>
            <activation>
                <activeByDefault>false</activeByDefault>
            </activation>
            <modules>
                <module>NativeExecutable</module>
            </modules>
        </profile>
    </profiles>


</project>
